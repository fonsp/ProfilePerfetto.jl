var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Exported","page":"API reference","text":"","title":"Exported"},{"category":"section","location":"api/#Index","page":"API reference","text":"","title":"Index"},{"category":"macro","location":"api/#ProfilePerfetto.@perfetto","page":"API reference","text":"@perfetto expr [key=value ...]\n\nProfile expr and render the result as an interactive Perfetto flame chart, inline in a Pluto, VS Code, or Jupyter notebook.\n\nThe sampling rate is calibrated automatically: expr is run a few times, starting at a coarse 10 ms sampling delay, and the delay is sharpened on each round so the final, displayed run captures plenty of detail without spending all its time inside the sampler. You don't have to pick a delay by hand.\n\nwarning: Your code runs multiple times\nBecause of the calibration loop, expr is evaluated several times (up to max_rounds, default 6). Don't use @perfetto on code with observable side effects (mutating shared state, writing files, sending network requests, …) — wrap the workload in something idempotent first, or call perfetto_view on profile data you collected yourself.\n\nCalibration options\n\nTrailing key = value arguments tune the calibration loop:\n\ninitial_delay::Float64 (default 0.01, i.e. 10 ms) — sampling delay used for the first calibration round. The first round is meant to be coarse and cheap; it just measures how long expr takes.\nmin_delay::Float64 (default 1e-7, i.e. 0.1 μs) — hard floor on the sampling delay. Raise it if you see huge profile overhead on very fast workloads.\nbuffer_slots::Int (default 10_000_000, ~80 MB) — size of the profile sample buffer. The loop targets ~50 % fill; bump this if traces get truncated.\nmax_rounds::Int (default 6) — maximum number of times expr is run. Lower this if you want fewer repetitions.\nmax_inflation::Float64 (default 10.0) — largest allowed ratio of measured wall time to the overhead-free baseline. Calibration backs off the sampling delay so profiler overhead can't blow runtime up by more than this factor.\nmax_step::Float64 (default 8.0) — maximum factor by which the delay is allowed to shrink between consecutive rounds.\n\nExample\n\nusing ProfilePerfetto\n\n@perfetto my_expensive_function(args...)\n\n# Cap calibration at 4 rounds and don't sharpen below 10 μs:\n@perfetto my_expensive_function(args...) max_rounds=4 min_delay=1e-5\n\nSee also @perfetto_open for opening the chart in a browser, and perfetto_view / perfetto_open for visualizing profile data you collected yourself.\n\n\n\n\n\n","title":"ProfilePerfetto.@perfetto"},{"category":"macro","location":"api/#ProfilePerfetto.@perfetto_view","page":"API reference","text":"@perfetto expr [key=value ...]\n\nProfile expr and render the result as an interactive Perfetto flame chart, inline in a Pluto, VS Code, or Jupyter notebook.\n\nThe sampling rate is calibrated automatically: expr is run a few times, starting at a coarse 10 ms sampling delay, and the delay is sharpened on each round so the final, displayed run captures plenty of detail without spending all its time inside the sampler. You don't have to pick a delay by hand.\n\nwarning: Your code runs multiple times\nBecause of the calibration loop, expr is evaluated several times (up to max_rounds, default 6). Don't use @perfetto on code with observable side effects (mutating shared state, writing files, sending network requests, …) — wrap the workload in something idempotent first, or call perfetto_view on profile data you collected yourself.\n\nCalibration options\n\nTrailing key = value arguments tune the calibration loop:\n\ninitial_delay::Float64 (default 0.01, i.e. 10 ms) — sampling delay used for the first calibration round. The first round is meant to be coarse and cheap; it just measures how long expr takes.\nmin_delay::Float64 (default 1e-7, i.e. 0.1 μs) — hard floor on the sampling delay. Raise it if you see huge profile overhead on very fast workloads.\nbuffer_slots::Int (default 10_000_000, ~80 MB) — size of the profile sample buffer. The loop targets ~50 % fill; bump this if traces get truncated.\nmax_rounds::Int (default 6) — maximum number of times expr is run. Lower this if you want fewer repetitions.\nmax_inflation::Float64 (default 10.0) — largest allowed ratio of measured wall time to the overhead-free baseline. Calibration backs off the sampling delay so profiler overhead can't blow runtime up by more than this factor.\nmax_step::Float64 (default 8.0) — maximum factor by which the delay is allowed to shrink between consecutive rounds.\n\nExample\n\nusing ProfilePerfetto\n\n@perfetto my_expensive_function(args...)\n\n# Cap calibration at 4 rounds and don't sharpen below 10 μs:\n@perfetto my_expensive_function(args...) max_rounds=4 min_delay=1e-5\n\nSee also @perfetto_open for opening the chart in a browser, and perfetto_view / perfetto_open for visualizing profile data you collected yourself.\n\n\n\n\n\n","title":"ProfilePerfetto.@perfetto_view"},{"category":"macro","location":"api/#ProfilePerfetto.@perfetto_open","page":"API reference","text":"@perfetto_open expr [key=value ...]\n\nLike @perfetto, but opens the resulting flame chart in your default web browser instead of rendering inline. Use this from the plain Julia REPL.\n\nThe expression is executed multiple times as part of automatic sampling rate calibration; see @perfetto for details and for the list of accepted key = value calibration options.\n\n\n\n\n\n","title":"ProfilePerfetto.@perfetto_open"},{"category":"function","location":"api/#ProfilePerfetto.perfetto_view","page":"API reference","text":"perfetto_view(data = Profile.fetch(; include_meta = false),\n                     lidict = Profile.getdict(data);\n                     name = \"Julia profile\")\n\nConverts Julia profile sample data to an embedded Perfetto trace viewer. Returns a PerfettoDisplay that renders as an interactive trace when displayed in a Pluto, VS Code or Jupyter notebook cell.\n\nSee also: @perfetto, perfetto_open.\n\n\n\n\n\n","title":"ProfilePerfetto.perfetto_view"},{"category":"function","location":"api/#ProfilePerfetto.perfetto_open","page":"API reference","text":"perfetto_open(data = Profile.fetch(; include_meta = false),\n                     lidict = Profile.getdict(data);\n                     name = \"Julia profile\")\n\nOpens the current Julia profile in the default web browser using the Perfetto trace viewer. Returns the path to the temporary HTML file that was opened.\n\n\n\n\n\n","title":"ProfilePerfetto.perfetto_open"},{"category":"function","location":"api/#ProfilePerfetto.perfetto_json","page":"API reference","text":"perfetto_json(data = Profile.fetch(; include_meta = true),\n                     lidict = Profile.getdict(data);\n                     filter_sentinel = false,\n                     wall_time_ns = nothing)\n\nConvert Julia profile sample data to a Perfetto trace-event JSON string and return it as a String. Useful for saving the trace to a file or piping it to another tool.\n\nSee also: perfetto_view, perfetto_open.\n\n\n\n\n\n","title":"ProfilePerfetto.perfetto_json"},{"category":"type","location":"api/#ProfilePerfetto.PerfettoDisplay","page":"API reference","text":"PerfettoDisplay\n\nReturned by perfetto_view. Renders as an embedded Perfetto trace viewer when displayed in a Pluto, VS Code or Jupyter notebook cell.\n\n\n\n\n\n","title":"ProfilePerfetto.PerfettoDisplay"},{"category":"section","location":"getting-started/#Getting-started","page":"Getting started","text":"Let's pretend you've written a function that feels slow. We'll profile it, read the flame chart, and find the bottleneck. No prior profiling experience required.","title":"Getting started"},{"category":"section","location":"getting-started/#Step-1-—-Something-to-profile","page":"Getting started","text":"Paste this into a Julia session (ideally a Pluto, VS Code, or Jupyter notebook):\n\nusing ProfilePerfetto\n\nfunction count_primes(n)\n    count = 0\n    for i in 2:n\n        if is_prime(i)\n            count += 1\n        end\n    end\n    return count\nend\n\nfunction is_prime(n)\n    n < 2 && return false\n    for d in 2:(n-1)         # a deliberately naive loop\n        n % d == 0 && return false\n    end\n    return true\nend","title":"Step 1 — Something to profile"},{"category":"section","location":"getting-started/#Step-2-—-Profile-it","page":"Getting started","text":"@perfetto count_primes(50_000)\n\nThat's it. Behind the scenes, @perfetto:\n\nRuns your expression once at a coarse sampling rate to see how long it takes.\nRuns it again with a sharper sampling rate so the flame chart has plenty of detail without being dominated by profiler overhead.\nHands the resulting samples to Perfetto and renders an interactive chart inline in your notebook.\n\nwarning: Your code runs multiple times\nBecause of the calibration loop, @perfetto evaluates the expression several times. Don't use it on code with side effects — see Profiling pre-collected data below.","title":"Step 2 — Profile it"},{"category":"section","location":"getting-started/#Step-3-—-Reading-the-flame-chart","page":"Getting started","text":"You'll see a horizontal timeline. Time flows left-to-right; each row is a stack frame. The function at the top of a stack was the one actually running when the sampler fired — that's where the time is going.\n\nThings you can do:\n\nAction How\nPan Click and drag, or use A / D\nZoom Scroll, or use W / S\nSee details Click a bar — file and line appear at the bottom\nSelect a range Shift-drag — total time & counts in sidebar\n\nIn our prime-counting example, you'll see is_prime dominating the chart, with most of the time spent inside its inner for loop. That's the smoking gun: replace 2:(n-1) with 2:isqrt(n) and re-run.","title":"Step 3 — Reading the flame chart"},{"category":"section","location":"getting-started/#Step-4-—-Re-run-and-confirm","page":"Getting started","text":"After your fix:\n\n@perfetto count_primes(50_000)\n\nThe is_prime bar should shrink dramatically. Profiling isn't just for finding problems — it's also how you confirm you fixed them. 🎯","title":"Step 4 — Re-run and confirm"},{"category":"section","location":"getting-started/#Which-macro-to-use?","page":"Getting started","text":"@perfetto expr / @perfetto_view expr — profiles expr and renders the chart inline in a Pluto, VS Code, or Jupyter notebook. @perfetto is an exported alias for @perfetto_view.\n@perfetto_open expr — profiles expr and opens the chart in your default web browser. Use this in the plain REPL.\n\nBoth macros accept the same trailing key = value calibration options — see Tuning the profiler.","title":"Which macro to use?"},{"category":"section","location":"getting-started/#Profiling-pre-collected-data","page":"Getting started","text":"If your workload has side effects, or you already have profile data — maybe collected with Profile.@profile directly, or from a long-running session — skip the macro and drive Profile yourself:\n\nusing Profile, ProfilePerfetto\n\nProfile.clear()\nProfile.@profile my_workload()        # runs exactly once\n\nperfetto_view()                       # or perfetto_open() in the REPL\n\nSee Using Profile.@profile manually for the full walkthrough.","title":"Profiling pre-collected data"},{"category":"section","location":"getting-started/#Common-gotchas","page":"Getting started","text":"warning: The first run includes compilation time\nJulia compiles each method the first time it runs. That compilation shows up in your flame chart as jl_type_infer, Core.Compiler.*, or similar. @perfetto's calibration loop runs your code several times, so by the final, displayed run things are usually warm — but if you want to be sure, run your workload once before profiling.\n\ntip: Threaded code shows up on separate rows\nEach OS thread gets its own track in the chart. Great for spotting one thread that's twiddling its thumbs while another carries the load.","title":"Common gotchas"},{"category":"section","location":"tuning/#Tuning-the-profiler","page":"Tuning the profiler","text":"@perfetto calibrates the sampling rate automatically, so for most code you don't need to tune anything. This page covers the knobs you can reach for when the defaults aren't quite right, and how to bypass the calibration loop entirely when your workload has side effects.","title":"Tuning the profiler"},{"category":"section","location":"tuning/#How-the-auto-calibration-works","page":"Tuning the profiler","text":"@perfetto runs your expression a few times. The first round uses a coarse 10 ms sampling delay just to time the workload. Each subsequent round sharpens the delay so that the profile buffer fills to roughly half, while keeping profiler overhead bounded. Only the final run is shown.\n\nThat means:\n\nShort workloads automatically get a sub-millisecond sampling rate.\nLong workloads automatically get a coarser rate, so your browser doesn't choke rendering millions of slices.\nYou don't have to call Profile.init yourself.","title":"How the auto-calibration works"},{"category":"section","location":"tuning/#Calibration-knobs","page":"Tuning the profiler","text":"You can pass trailing key = value arguments to @perfetto (or @perfetto_open) to nudge the calibration:\n\nusing ProfilePerfetto\n\n# Cap calibration at 4 rounds and don't sharpen below 10 μs:\n@perfetto my_workload() max_rounds=4 min_delay=1e-5\n\n# Bigger sample buffer if your traces are getting truncated:\n@perfetto my_workload() buffer_slots=50_000_000\n\nThe full list:\n\ninitial_delay::Float64 (default 0.01, i.e. 10 ms) — sampling delay used for the first calibration round.\nmin_delay::Float64 (default 1e-7, i.e. 0.1 μs) — hard floor on the sampling delay. Raise it if you're seeing huge profile overhead on very fast workloads.\nbuffer_slots::Int (default 10_000_000, ~80 MB) — size of the profile sample buffer. The loop targets ~50 % fill; bump this if your traces are getting truncated.\nmax_rounds::Int (default 6) — maximum number of times the expression is run during calibration.\nmax_inflation::Float64 (default 10.0) — largest allowed ratio of measured wall time to the overhead-free baseline. Calibration backs off the sampling delay so profiler overhead can't blow runtime up by more than this factor.\nmax_step::Float64 (default 8.0) — maximum factor by which the delay is allowed to shrink between consecutive rounds.","title":"Calibration knobs"},{"category":"section","location":"tuning/#Side-effects:-skip-the-macro","page":"Tuning the profiler","text":"The calibration loop runs your expression multiple times. If that's not acceptable — your code mutates shared state, writes to a file, hits a network — drive Profile yourself and pass the data to perfetto_view or perfetto_open:\n\nusing Profile, ProfilePerfetto\n\nProfile.init(n = 10^7, delay = 0.0001)   # 100 μs → 10 kHz\nProfile.clear()\nProfile.@profile my_workload()           # runs exactly once\n\nperfetto_view()                          # in a notebook\n# or\nperfetto_open()                          # opens in your browser\n\nProfile.init's parameters are documented in the Profile stdlib docs:\n\ndelay — seconds between samples. Default is 0.001 (1 ms). Smaller = finer-grained chart, more overhead, fills the buffer faster.\nn — maximum number of instruction-pointer slots in the sample buffer.","title":"Side effects: skip the macro"},{"category":"section","location":"tuning/#Including-C-frames","page":"Tuning the profiler","text":"By default Julia hides internal C frames (libuv, GC internals, etc.). If you're debugging an FFI call or want to see where garbage collection is eating your time, fetch profile data manually:\n\nusing Profile, ProfilePerfetto\n\nProfile.clear()\nProfile.@profile my_workload()\n\ndata   = Profile.fetch(; include_meta = true)\nlidict = Profile.getdict(data)           # default hides C frames; pass C = true to include\n\nperfetto_view(data, lidict)","title":"Including C frames"},{"category":"section","location":"tuning/#More-from-the-Profile-stdlib","page":"Tuning the profiler","text":"Everything in the Profile stdlib docs composes with ProfilePerfetto. A few highlights worth knowing:\n\nProfile.print() — text dump of the same samples ProfilePerfetto visualizes.\nProfile.Allocs — allocation profiler (tracks where you allocate, not where you spend CPU time). ProfilePerfetto only handles the CPU profiler.\nProfile.fetch(; include_meta = true) — raw sample data, ready to pass into perfetto_view or perfetto_open.","title":"More from the Profile stdlib"},{"category":"section","location":"manual-profile/#Using-Profile.@profile-manually","page":"Using Profile.@profile manually","text":"The @perfetto macro is the easy path: it runs your code, picks a good sampling rate, and renders the chart in one step. But sometimes you need more control:\n\nYour workload has side effects and shouldn't be executed multiple times by the auto-calibration loop.\nYou want to profile a long-running session and visualize whatever samples have accumulated so far.\nYou're collecting samples in one process (e.g. a server) and want to view them somewhere else.\nYou want to use Profile's own knobs — Profile.init, Profile.@profile, @profile sample_rate=…, etc. — directly.\n\nIn all of these cases, collect samples with the Profile stdlib yourself, then hand them to perfetto_view (for in-notebook display) or perfetto_open (for the browser).","title":"Using Profile.@profile manually"},{"category":"section","location":"manual-profile/#The-basic-recipe","page":"Using Profile.@profile manually","text":"using Profile, ProfilePerfetto\n\nProfile.clear()\nProfile.@profile my_workload()        # runs exactly once\n\nperfetto_view()\n\nThat's it. With no arguments, perfetto_view (and perfetto_open) read whatever samples are currently sitting in Julia's profile buffer — the same buffer that Profile.@profile just wrote to.\n\n# In a notebook (Pluto / VS Code / Jupyter):\nperfetto_view()\n\n# In the plain REPL, or whenever you want a full browser tab:\nperfetto_open()","title":"The basic recipe"},{"category":"section","location":"manual-profile/#Tuning-the-sample-rate","page":"Using Profile.@profile manually","text":"Auto-calibration is what @perfetto does for you. When you drive Profile yourself, you pick the rate. The default is one sample every 1 ms, which is often too coarse for fast workloads and too fine for long ones.\n\nusing Profile, ProfilePerfetto\n\n# Sample every 100 µs — good for workloads in the millisecond range.\nProfile.init(; delay = 0.0001)\n\nProfile.clear()\nProfile.@profile my_workload()\n\nperfetto_view(; name = \"my_workload, 100µs sampling\")\n\nRule of thumb: aim for a few hundred to a few thousand samples per run. Fewer than ~50 and the chart is too sparse to read; more than ~100,000 and the buffer fills up and Perfetto starts to chug.\n\nSee Tuning the profiler for more on choosing a rate.","title":"Tuning the sample rate"},{"category":"section","location":"manual-profile/#Passing-data-explicitly","page":"Using Profile.@profile manually","text":"If you've already pulled samples out of the profile buffer — for example to save them to disk, or to ship them between processes — pass them in directly:\n\ndata   = Profile.fetch(; include_meta = true)\nlidict = Profile.getdict(data)\n\nperfetto_view(data, lidict; name = \"Today's run\")\n\nwarning: Always use `include_meta = true`\nProfilePerfetto needs the per-sample metadata (thread id, task id, timestamps) that Julia's Profile.fetch only emits when include_meta = true. Without it, the parser can't reconstruct the timeline.","title":"Passing data explicitly"},{"category":"section","location":"manual-profile/#Profiling-a-long-running-session","page":"Using Profile.@profile manually","text":"You don't have to stop sampling before viewing. Call perfetto_view() whenever you want a snapshot — the profile buffer keeps accumulating in the background.\n\nProfile.init(; delay = 0.001, n = 10_000_000)   # bigger buffer\nProfile.clear()\nProfile.@profile run_server()        # runs for a while…\n\n# Later, in another cell or after Ctrl-C:\nperfetto_view()","title":"Profiling a long-running session"},{"category":"section","location":"manual-profile/#See-also","page":"Using Profile.@profile manually","text":"perfetto_view — render in a notebook.\nperfetto_open — open in the browser.\nTuning the profiler — picking a sample rate.\nThe Julia Profile stdlib docs.","title":"See also"},{"category":"section","location":"#ProfilePerfetto.jl","page":"Home","text":"Profile your Julia code and view the results in a beautiful, interactive flame chart. 🔥\n\n<p><img alt=\"Screenshot of a Perfetto traces\" src=\"./assets/perfetto traces.png\" style=\"max-width: 500px\"></p>\n\nProfilePerfetto.jl is an easy-to-use profiling package for Julia, using the built-in Profile standard library and the Perfetto trace viewer — the same tool used for analyzing Chrome and Android performance traces.\n\nBy default, ProfilePerfetto will also find the right sampling rate automatically, by running your code multiple times and calibrating itself. ","title":"ProfilePerfetto.jl"},{"category":"section","location":"#What-is-profiling?","page":"Home","text":"If you want to make your code faster, you need to know exactly which parts are slow (so that you can work on the most impactful ones). With profiling, you run a piece of code while the Julia profiler samples the stack many times per second, recording what function is currently running at each sample. \n\nJulia has a built-in statistical profiler in the Profile stdlib. This package uses the built-in profiler to collect samples, then displays them in the Perfetto UI.","title":"What is profiling?"},{"category":"section","location":"#A-30-second-example","page":"Home","text":"using ProfilePerfetto\n\nfunction slow_thing()\n    x = 0.0\n    for i in 1:10_000_000\n        x += sin(i) * cos(i)\n    end\n    return x\nend\n\n@perfetto slow_thing()\n\nRun that cell in Pluto, VS Code, or Jupyter, and you'll see an interactive flame chart appear right below it. Drag to pan, scroll to zoom, click on a bar to see the file and line number.\n\ntip: Not in a notebook?\nFrom the plain Julia REPL, use @perfetto_open instead — it opens the chart in your default browser.\n\n@perfetto automatically calibrates the sampling rate for you — there's no delay knob to tune. The trade-off: it runs your code a few times to do so.\n\nwarning: Your code runs multiple times\n@perfetto evaluates the expression several times while calibrating. If it has observable side effects (mutating state, I/O, network calls), wrap the workload in something idempotent first, or hand pre-collected profile data to perfetto_view.","title":"A 30-second example"},{"category":"section","location":"#What's-next?","page":"Home","text":"Getting started — a friendly walk-through for anyone new to profiling.\nTuning the profiler — calibration knobs and how to feed in your own profile data.\nAPI reference — every exported name, spelled out.","title":"What's next?"}]
}
